# QA 자동화 하네스 (OpenArena) — 프로세스 기록

## 0. 문서 작성 규칙
- 이 문서는 프로젝트의 의의, 방향, 진행 과정, 결정, 변경 사항을 시간순으로 **누적** 기록한다.
- 내용이 바뀌어도 기존 기록을 **삭제하지 않는다**. 대신 해당 항목에 취소선을 긋고 `[제거 YYYY-MM-DD: 사유]`를 표시한 뒤, 바뀐 내용을 아래에 `[추가 YYYY-MM-DD]`로 새로 기록한다.
- 예시: ~~기존 결정 내용~~ `[제거 2026-07-20: 방향 변경]` → 아래에 새 결정을 추가한다.
- 문서체는 평서형(~다)으로 통일한다.

---

## 1. 프로젝트 의의
- 목적: OpenArena를 대상으로 게임 QA 자동화의 실제 예시를 구현하고, 발표로 시연 가능한 형태로 보여준다.
- 핵심 주장: 봇이 게임을 플레이하는 동안 시스템이 자동으로 버그를 탐지하고, 그 결과를 사람이 검토할 수 있도록 누적·시각화한다.
- OpenArena를 대상으로 선택한 이유는 다음과 같다.
  - 오픈소스 C++라 소스 계측과 버그 주입이 가능하다.
  - 봇이 내장돼 있어 정상 플레이 데이터를 확보하기 쉽다.
  - 콘솔·데모·timedemo 등 자동화에 활용할 자원이 갖춰져 있다.
  - 저사양 하드웨어(Intel HD 4600)에서도 구동된다.

---

## 2. 핵심 설계 원칙: 액터와 오라클의 분리
- **액터(actor)**: 게임 안에서 실제로 움직이며 상태를 생성하는 주체다. (내장 봇 / 스크립트 봇 / 강화학습 에이전트)
- **오라클(oracle)**: 특정 상태가 버그인지 판정하는 검출기다.
- 이 둘은 독립적인 축이다. 강화학습은 액터 선택지일 뿐, 그 자체로 버그를 판정하지 않는다.
- 따라서 "정상 플레이 학습 기반 이상탐지" 대 "강화학습"은 양자택일 관계가 아니다. 어떤 액터를 쓰든 오라클은 별도로 필요하다.
- 강화학습이 QA에 기여하는 지점은 오직 **탐색 범위(coverage)** 확대다. 다만 이기도록 학습된 에이전트는 버그 상태를 회피하므로, 버그를 찾게 하려면 보상을 "새로운/희귀한 상태 도달"로 설계해야 한다.

---

## 3. 아키텍처
전체 흐름은 다음과 같다.

    액터 → 수집 → 오라클(3계층) → 집계 → 대시보드(사람 검토)

### 3.1 액터 층
- OpenArena 게임 엔진과 봇으로 구성한다.
- 평가와 시연을 위해 재현 가능한 합성 버그를 소스에 주입한다.

### 3.2 수집 층
- 텔레메트리: 봇의 위치·속도·체력·시야각·탄약·이벤트를 매 틱 수집한다.
- 로그 / 이벤트 스트림을 수집한다.
- 프로세스 지표: CPU·메모리·생존 여부를 수집한다.

### 3.3 오라클 층 (3계층)
- **① 하드 인바리언트(결정적 규칙)**: Z좌표가 맵 바닥 아래, 체력 범위 초과, 좌표 경계 밖(OOB), 일정 시간 위치 불변(끼임) 등을 판정한다. 오탐이 거의 없고 설명이 명확하다.
- **② 이상탐지(비지도 ML)**: 정상 봇 플레이로 학습하여 편차를 탐지한다. 규칙은 어기지 않지만 이상한 행동을 잡는다. 오탐이 있어 사람 검토가 필요하다.
- **③ 크래시/프로세스 워치독**: 크래시·행(멈춤)·메모리 증가를 게임 밖에서 감시한다.

### 3.4 집계 층
- 세 오라클의 출력을 모아 버그 목록으로 누적 저장한다.

### 3.5 표현 층
- 라이브 대시보드에 게임 플레이 화면, 실시간 이벤트 로그, 누적 버그 테이블(타임스탬프·유형·심각도·스크린샷)을 표시한다.
- 사람이 대시보드를 검토한다(human-in-the-loop).

---

## 4. 결정 로그
- 2026-07-20: 대상 게임을 OpenArena로 확정한다.
- 2026-07-20: 오라클을 3계층(하드 인바리언트 + 이상탐지 + 크래시/프로세스 감시)으로 구성하기로 한다.
- 2026-07-20: v1에서는 강화학습을 채택하지 않고 보류한다. 사유는 다음과 같다.
  - (1) 강화학습은 액터일 뿐 버그 판정기가 아니다.
  - (2) 이기도록 학습된 에이전트는 버그 상태를 회피하므로 테스터로 부적합하다.
  - (3) OpenArena에는 즉시 쓸 수 있는 RL 인터페이스가 없고, 학습 비용이 크며, 현재 하드웨어에서 비현실적이다.
  - 강화학습은 향후 "탐색 커버리지 확장"으로 후순위에서 재검토한다.
- 2026-07-20: 합성 버그 주입 방식을 "cvar로 토글하는 결함 주입기를 게임 모듈(qagame) 코드에 심는 방식"으로 확정한다. 세부 방침은 다음과 같다.
  - 엔진이 아니라 게임 모듈만 수정·빌드하며, 결함 주입기는 텔레메트리 계측 훅과 같은 층에 둔다.
  - `qa_inject_*` cvar로 각 버그 유형을 개별 토글해 발표 때 정해진 시점에 재현 가능하게 한다.
  - 내가 언제 무엇을 켰는지 알 수 있으므로, 주입한 버그는 정답이 붙은 평가 데이터로 활용한다.
  - 빠른 데모용으로 기존 cvar(예: g_gravity, g_speed)를 보조 수단으로 쓴다.
- 2026-07-20: 대상 게임 소스를 `OpenArena/gamecode`(OAX)로 확정하고, 게임 로직을 네이티브 DLL이 아니라 **QVM 모드**로 빌드하기로 한다. 사유는 QVM이 아키텍처 독립적이라 스톡 0.8.8 엔진을 그대로 쓸 수 있고, 동봉된 lcc/q3asm 도구로 빌드해 최신 gcc와 옛 코드의 충돌 문제도 피하기 때문이다. (상세: docs/injection_map.md 5-B)
- 2026-07-20: 결함 주입과 텔레메트리 계측을 모두 `g_active.c`의 `ClientEndFrame`, 체력 동기화(`ps.stats[STAT_HEALTH] = ent->health`) 직후에 둔다. 그 자리가 상태가 프레임마다 확정되는 지점이라, 주입값이 최종 상태로 살아남고 계측도 그 확정값을 읽을 수 있기 때문이다.
- 2026-07-20: 텔레메트리는 QVM 제약상 직접 파일 입출력을 못 하므로 엔진 syscall(`trap_FS_FOpenFile`/`trap_FS_Write`/`trap_FS_FCloseFile`)로 파일에 쓴다. 형식은 `StateSample` 스키마와 동일한 JSONL로 하여, 기존 `replay_source`/`run_invariants`가 그대로 소비할 수 있게 한다. 봇(`SVF_BOT`)만 기록한다.
- 2026-07-20: 체력 하드 인바리언트를 실제 게임 규칙에 맞게 교정한다. 배경과 사유는 다음과 같다(실제 텔레메트리 검증 중 발견한 오탐).
  - 발견: `qa_inject_health`가 꺼진 정상 플레이에서도 `health_out_of_range`가 잡혔다. 예: 체력 95, max_health 70.
  - 원인: 검출기 규칙이 게임의 실제 규칙과 어긋났다. OpenArena에서는 체력이 max_health를 넘는 것이 정상이다. `g_client.c`에서 스폰 시 체력을 `max_health + 25`로 주고(70이면 95로 시작), `g_items.c` 주석대로 소형/메가헬스는 "will go over the max"이며, 가드 파워업은 최대 `2*handicap`(핸디캡 100이면 200)까지 올린다. 즉 정당한 체력 상한은 약 200이다.
  - 교훈: 하드 인바리언트는 "드물다"가 아니라 "물리적으로 불가능하다"를 담아야 한다. `health > max_health`는 정상 오버힐을 오탐하므로 부적합했다. "최대치를 약간 넘는" 애매한 영역은 하드 규칙이 아니라 이상탐지(ML)의 몫이다.
  - 수정: 규칙을 '절대 상한 초과'로 변경했다(`health > max_possible_health`, 기본 250). 정당한 상한(~200)보다 넉넉히 위, 주입 오버플로우(999)보다 아래로 잡아 정상 오버힐은 통과시키고 진짜 오버플로우만 잡는다. 하한(`health < 0`)도 사망 시 음수가 정상이라 오탐 소지가 있어 제거했다.
  - 검증: 실제 텔레메트리에서 정상 구간의 체력 95 등이 더 이상 잡히지 않고, 주입값 999만 잡히는 것을 확인했다.
- 2026-07-20: 맵 경계(`MapBounds`)는 게임 내부 데이터가 아니라 **정상 플레이 관측**에서 역산하기로 한다(`tools/calibrate_bounds.py`). 근거는 다음과 같다.
  - 게임(`.bsp`)도 맵 경계 데이터(worldmodel의 mins/maxs)를 갖지만, 그것은 걸어다닐 수 있는 영역이 아니라 맵 기하 전체(벽 바깥·스카이박스 포함)를 감싸는 큰 상자다. 이걸 기준으로 삼으면 봇이 벽을 뚫고 나가도 그 큰 상자 안이라 경계 이탈을 놓친다. QA가 원하는 것은 "기하의 끝"이 아니라 "봇이 정상적으로 있을 수 있는 영역의 끝"이며, 둘은 다르다.
  - game-agnostic 원칙과의 일관성. 경계를 게임 내부 API로 뽑으면 맵마다 게임 내부에 결합되어 원칙이 흐려진다. 관측 방식은 텔레메트리만으로 경계를 정한다.
  - 한계: 관측 방식은 봇이 아직 안 가본 정상 영역을 경계 밖으로 오판할 수 있으므로, 충분히 오래 수집하거나 여유(margin, 현재 20%)를 둔다. 필요 시 `.bsp` 경계를 절대 한계로, 관측 경계를 정상 범위로 쓰는 이중 판정으로 확장할 수 있다.
- 2026-07-20: 대시보드 방식을 확정했다.
  - 프론트엔드는 리액트(Vite)로, 백엔드는 파이썬 FastAPI로 한다. 리액트는 브라우저에서 도는 클라이언트(화면)이고, 파이썬 서버가 데이터(검출 결과)를 준다. 전부 localhost 전용이라 인터넷과 무관하게 동작한다.
  - 게임 화면은 대시보드 안에 넣지 않는다. 브라우저에 게임 영상을 넣으려면 캡처·스트리밍이 필요해 복잡하고, 녹화 영상을 로그와 시점 맞춰 트는 방식은 "실제 탐지"가 아니라 짜맞춤처럼 보여 정직하지 않다. 대신 게임을 **별도 창**으로 대시보드 옆에 띄운다. 두 창이 각각 실제로 돌고 그 사이를 텔레메트리가 실제로 흐르므로 정직하고 구현도 단순하다. 최종 시연은 두 창을 함께 화면 녹화한다.
  - 데이터 전달은 파일 tail 방식으로 시작한다(파이썬이 파일 끝을 주기적으로 읽고 프론트엔드가 폴링). 0.1~0.5초 지연은 탐지가 영상이 아니라 사건이라 문제되지 않는다. 실시간 최적화(WebSocket 푸시)는 나중 선택지로 남긴다.
  - 화면은 3열 구조로 한다. 좌: 규칙별 집계 + 이상탐지 자리, 중앙: 실시간 탐지 로그, 우: 클릭 시 열리는 상세 보기 패널. 의미 없는 통계(총계·심각·주의·분당 탐지)는 프레임 단위 중복 때문에 실제 버그 수와 무관해 오해를 부르므로 넣지 않는다. 상세 보기를 인라인 확장이 아니라 옆 패널로 둔 것은, 이상탐지 상세(점수·기여 특징·그래프)가 세로로 길어 실시간 로그 흐름을 방해하지 않게 하기 위해서다.
  - 집계(프레임 중복을 하나의 버그 사건으로 묶기)는 검출기가 아니라 서버/대시보드 층에서 한다. 검출기는 매 프레임 판정만 하고, 중복 묶기는 표현의 문제이기 때문이다.
- 2026-07-20: 하드 인바리언트의 맵 의존성은 알려진 한계로 둔다.
  - 다섯 규칙 중 경계·바닥·과속은 맵마다 다른 값(경계·속도 상한)에 의존하므로 맵이 바뀌면 재보정이 필요하다. 체력·끼임은 맵과 무관하다.
  - 포트폴리오에서는 한 맵(예: q3dm17) 데모로 충분하므로 지금 다맵 지원은 하지 않는다.
  - 확장 방향: (1) 맵별 설정 파일에 보정값을 저장해 재사용, (2) 텔레메트리에 현재 맵 이름 필드를 추가해 서버가 자동으로 맞는 설정을 선택, (3) 관측 기반 자동 학습으로 맵 의존성을 데이터로 흡수(이상탐지와 연결). 지금은 한계·확장 방향으로만 남긴다.
- 2026-07-22: 서버 응답과 화면 표기의 필드 이름 불일치를 **프론트엔드의 변환 함수(`normalizeDetection`)로 흡수**하기로 한다.
  - 발견: 서버는 `severity`/`message`/`entity_id`로 내보내는데 대시보드는 `sev`/`msg`/`entity`로 읽고 있었다. mock 시절에 프론트엔드가 자기 편한 이름을 쓴 결과다. fetch만 붙여서는 화면이 채워지지 않는다.
  - 결정: 서버 쪽 이름을 화면에 맞춰 줄이지 않는다. 서버 응답은 화면 전용 포맷이 아니라 API 계약이며, 이상탐지 오라클(②)이 붙으면 같은 응답을 다른 소비자(로그 저장·리포트)도 소비하게 된다. 표기 편의로 계약 쪽 이름을 줄이면 의미가 흐려진다.
  - 이점: 변환이 한 함수에 모여 있으므로 `kind: "anomaly"` 항목이 섞여 들어와도 그 자리만 손보면 된다.
- 2026-07-22: 대시보드의 LIVE 표시와 흐르는 세션 시계를 제거하고 **연결 상태 + 다시 불러오기 버튼**으로 대체한다.
  - 사유: 현 단계는 파일 전체를 한 번에 검출한 정적 스냅샷이다. 멈춰 있는 데이터 위에서 LIVE 램프가 깜빡이고 시계가 흘러가면 실제보다 실시간처럼 보인다. 이는 "녹화 영상을 로그와 시점 맞춰 트는 방식은 짜맞춤처럼 보여 정직하지 않다"고 판단한 것과 같은 이유로 배제한다.
  - 헤더의 시간 표시는 흐르는 시계가 아니라 **텔레메트리의 마지막 게임 시간**으로 바꾼다. 데이터가 실제로 끝난 지점을 가리키므로 사실과 일치한다.
  - 실시간 표시는 서버가 파일 끝만 읽어 새 줄을 흘려보내는 단계에서 되살린다. 그때는 표시가 사실이 된다.
- 2026-07-22: 탐지 로그에 **실제로 그리는 행 수를 최근 300건으로 제한**한다.
  - 사유: 하드 인바리언트는 매 프레임 판정하므로 한 번의 버그 사건이 수백~수천 건으로 불어난다. 전부 DOM 행으로 그리면 Intel HD 4600 환경에서 브라우저 렌더가 먼저 병목이 된다.
  - 총 건수와 규칙별 집계는 자르기 전 전체 목록으로 계산하므로 숫자는 왜곡되지 않는다. 잘린 경우에는 "총 N건 중 최근 300건만 표시한다"고 화면에 밝힌다.
  - 사건 단위 집계가 서버 층에 붙으면 이 제한은 다시 검토한다.
- 2026-07-22: 대시보드에서 mock 잔재(봇 이름 배열 `BOTS`, 하드코딩된 맵 이름 `q3dm17`)를 제거한다.
  - 사유: 텔레메트리에는 봇 이름도 맵 이름도 없다. 화면에만 있는 값은 데이터에 근거가 없으므로 시연에서 오해를 부른다.
  - 조치: 봇은 엔티티 번호(`BOT 3`)로만 표시하고, 맵은 "미기록"으로 표시한다. 헤더의 봇 수는 하드코딩이 아니라 탐지 목록에서 센 고유 엔티티 수로 바꿨다.
  - 정당한 해법은 텔레메트리에 맵 이름 필드를 추가하는 것이며, 이는 맵 의존성 확장 방향(2026-07-20 항목)과 같은 작업이다.
- 2026-07-22: 대시보드에 **연결 실패 시에만 동작하는 자동 재시도**를 넣고, 주기 폴링(자동 데이터 갱신)은 넣지 않는다.
  - 배경: 리액트를 먼저 켜고 서버를 나중에 켜자 화면이 "연결 실패"에 머물렀고, 버튼을 눌러야만 붙었다. `fetch`는 한 번 보내고 끝나는 요청이라 서버가 나중에 뜬 것을 알 방법이 없다. 이 순서는 앞으로도 반복되므로 고칠 가치가 있다.
  - 결정 1(자동 재시도): 서버에 **연결 자체가 안 되는 경우**(fetch 가 TypeError 로 실패)에만 3초 간격으로 다시 시도하고, 연결되면 멈춘다.
  - 결정 2(수동 유지): 서버가 **오류 응답(비정상 상태 코드)** 을 주는 경우는 자동 재시도하지 않는다. 서버는 살아 있고 안에서 뭔가 잘못됐다는 뜻이라, 자동으로 계속 요청하면 상황은 안 바뀌면서 문제만 가려진다. 사람이 서버 로그를 봐야 하는 상황이므로 수동 재시도로 남긴다. "연결 안 됨"과 "고장남"은 대응이 다르다는 것이 이 구분의 근거다.
  - 결정 3(폴링 보류): 연결된 뒤 주기적으로 데이터를 다시 받아오는 폴링은 지금 넣지 않는다. 현재 서버는 요청마다 텔레메트리 파일 전체를 다시 읽고 검출기를 다시 돌리므로, 파일이 커질수록 요청 한 번이 곧 전체 재검출이 된다. 저사양 환경에서 얻는 것에 비해 비용이 크다. 폴링은 서버가 파일 끝의 새 줄만 읽도록 바뀐 뒤에 넣는다. 그때는 요청 한 번이 새로 생긴 몇 줄만 처리하므로 폴링이 싸진다.
- 2026-07-22: 집계(프레임 중복을 버그 사건으로 묶기)의 방식을 확정했다.
  - **묶는 기준은 `(봇, 규칙)`이다.** 위치는 기준에 넣지 않는다. 봇이 바닥을 뚫고 떨어지는 동안 좌표가 계속 변하므로 위치를 기준에 넣으면 한 낙하가 수십 개 사건으로 쪼개진다. 심각도는 규칙에서 파생되므로 따로 넣을 필요가 없다.
  - **끊는 기준은 시간 간격(0.5초)이다.** 같은 `(봇, 규칙)`의 탐지가 이 간격 이내로 이어지면 한 사건, 그보다 벌어지면 새 사건이다. 근거: 텔레메트리는 서버 프레임마다 기록되므로 연속 구간의 간격은 프레임 주기(20Hz면 0.05초)다. 0.5초는 약 10프레임이라, 봇이 경계선 위에서 들락날락해 탐지가 한두 프레임 깜빡이는 경우를 하나로 묶으면서 실제로 따로 일어난 두 사건은 분리한다.
  - 한계: 이 0.5초는 관측에서 역산한 값이 아니라 정한 상수다. 맵 경계를 보정했던 것처럼 실제 텔레메트리의 프레임 간격을 재서 그 배수로 잡는 편이 정확하다. 확장 항목으로 남긴다.
  - **사건이 담는 정보**: 봇·규칙·심각도, 시작/끝 시각, 지속 시간, 프레임 수, 시작 순간의 상태값, 규칙별 극값과 그 순간의 상태값, 진행 중 여부.
  - **극값은 규칙별 표로 처리한다.** 각 규칙이 이미 `Bug.details`에 자기가 위반한 수치를 넣고 있으므로, 집계기는 규칙의 내용을 모른 채 "규칙 이름 → (details 키, 최대/최소)" 표만 보고 극값을 추적한다. 바닥 관통은 최저 z, 체력은 최대 체력, 과속은 최대 속력, 끼임은 최대 정지 시간이다. 표에 없는 규칙이 와도 극값만 비고 나머지 집계는 정상 동작한다.
  - **주 증거는 시작 순간의 상태로 둔다.** 무엇이 잘못되기 시작했는지가 진단의 출발점이기 때문이다. 극값은 심각도를 가늠하는 보조 정보로 함께 싣는다.
  - **실시간 대비**: 집계기를 "파일 전체를 받아 한 번에 묶는 함수"가 아니라 **탐지를 하나씩 받는 상태 기계**로 만든다. 지금은 파일 전체를 먹이고, 서버가 파일 끝의 새 줄만 읽게 되면 새로 읽은 것만 먹인다. 두 경우에 집계기 코드는 동일하다. 집계를 실시간보다 먼저 한 이유가 이것이다.
  - **'진행 중' 표시**: 스트림이 끝난 시점(`finalize`)에 아직 닫히지 않은 사건은 진행 중으로 표시한다. 정적 모드에서는 "파일이 사건 도중에 끝났다", 실시간 모드에서는 "지금 벌어지고 있다"는 뜻이 된다. 같은 코드가 두 의미를 모두 표현한다.
  - **층 구분**: 집계기는 `qa/aggregate.py`에 두되 `InvariantChecker`의 일부가 아니다. 층의 구분은 폴더가 아니라 호출 방향으로 정한다. 검출기는 집계를 부르지 않고, 소비자(서버·CLI)가 검출기 뒤에 집계를 붙인다.
  - **엔드포인트를 둘로 나눈다**: `/api/events`(집계 사건, 대시보드가 사용)와 `/api/detections`(집계 전 원시 탐지, 대조·검증용)를 모두 남긴다. 집계가 무엇을 어떻게 묶었는지 대조할 수 없으면 집계 결과를 신뢰할 근거가 없기 때문이다. 시연에서 "이 237건이 6개 사건으로 묶였다"를 보여줄 때도 쓴다.
  - 알려진 한계: (1) 같은 봇의 같은 규칙이 0.5초 안에 두 번 따로 일어나면 한 사건으로 합쳐진다(과소 집계 방향). (2) 바닥 관통과 과속처럼 원인·결과로 얽힌 사건은 여전히 따로 잡힌다. "이 과속은 저 낙하 때문"이라고 묶으려면 사건 간 상관관계 분석이 필요하며, 이번 범위 밖이다.
- 2026-07-22: `stuck` 규칙의 `reported` 플래그를 제거하고, 다른 규칙과 같이 위반이 지속되는 동안 매 틱 보고하게 한다.
  - 배경: `_check_stuck`만 자체 중복 억제를 하고 있었다. 한 번 끼임을 보고하면 봇이 다시 움직일 때까지 보고하지 않았다. 집계 층이 없던 시점에 검출기 안에서 중복을 막은 임시 조치였고, 당시로선 합리적이었다.
  - 문제 1: 집계 층이 생기면 중복 제거 장치가 서로 다른 두 곳에 존재하게 된다. "검출기는 매 프레임 판정만 하고 중복 묶기는 표현의 문제"라는 기존 결정(2026-07-20)과도 어긋난다.
  - 문제 2: 실질적 손해로, `stuck`만 사건이 항상 1프레임짜리가 되어 **지속 시간이 사라진다.** 2.1초 끼였다 풀린 것과 40초째 끼여 있는 것은 QA에서 전혀 다른 사안인데 구분되지 않는다.
  - 조치: 플래그를 제거했다. 원시 탐지 수는 늘지만 집계가 흡수하며, 끼임도 지속 시간과 최대 정지 시간을 갖는 사건이 된다.
- 2026-07-23: 메모리 관리 대상에 대한 앞선 판단(2026-07-22 논의 중 "사건 보관 수에 상한을 둔다")을 **철회한다.**
  - 잘못된 점 1: 버그를 기록하는 시스템이 오래된 버그부터 잊는 것은 본말전도다. 30분 세션에서 초반 사건이 사라져 있으면 QA 도구로서 실패다. 사건은 이미 프레임 중복이 묶인 뒤의 최소 단위이지 여유분이 아니다.
  - 잘못된 점 2: 수치를 세지 않고 걱정했다. 사건 하나는 스칼라 몇 개와 상태 스냅샷 두 개라 약 1.5~2 KB이고, 분당 10개가 나와도 한 시간에 1 MB 남짓이다. 16 GB에서 문제 될 규모가 아니었다.
  - **실제로 커지는 것은 원시 탐지다.** 봇 하나가 끼여 있으면 초당 20건씩 나오므로 한 시간이면 7만 건, 70 MB 규모다. 정적 서버는 요청마다 새로 만들어 버려 보이지 않았을 뿐, 서버가 상태를 들고 있게 되면 이쪽이 먼저 문제가 된다.
  - **판단 기준(이 사건에서 얻은 것): 재현 가능한 파생물은 버려도 되고, 시스템이 내린 결론은 버리면 안 된다.** 원시 탐지는 텔레메트리 파일만 있으면 `run_invariants.py`로 언제든 다시 만들 수 있는 파생물이다. 사건은 결론이다.
  - 조치: 사건은 전부 보관하고 파일로도 남긴다. 원시 탐지는 최근 500건만 링 버퍼로 유지하고 누적 개수는 세어서 노출한다. 응답 크기는 `limit`으로 제한하되(기본 300), 그것은 오가는 양을 줄이는 것이지 보관을 줄이는 것이 아니다.
- 2026-07-23: 실시간화의 구현 방식을 확정했다.
  - **읽기**: 파일 오프셋을 기억해 그 뒤부터만 읽는다. 요청 한 번의 비용이 '파일 전체'에서 '새로 생긴 줄'로 바뀌며, 이것이 1초 폴링을 감당 가능하게 만든다.
  - **반쯤 쓰인 줄**: 게임이 계속 덧붙이는 중이라 마지막 줄이 절반만 쓰여 있을 수 있다. 마지막 개행까지만 소비하고 남은 조각은 버퍼에 두었다가 다음 읽기에서 앞에 이어 붙인다. 이 처리를 빠뜨리면 시연 도중 무작위로 500이 나는 형태로 터진다.
  - **바이트로 읽는 이유**: 텍스트 모드에서는 여러 바이트로 이뤄진 문자가 중간에 잘렸을 때 디코딩이 실패한다. 바이트 버퍼에 담고 완결된 줄만 디코딩하면 그 문제가 사라진다.
  - **게임 재시작 감지**: 파일 크기가 기억하던 오프셋보다 작아지면 파일이 초기화된 것으로 보고 오프셋·검출기·집계기·기록기를 전부 새로 만든다. 옛 끼임 이력이 새 매치로 넘어가면 안 되기 때문이다. 한계: 파일이 잘리지 않고 이어 쓰이는 경우는 감지되지 않으므로 수동 `/api/reset`을 둔다.
  - **깨진 줄**: 한 줄 때문에 감시 전체가 멈추면 안 되므로 건너뛰되, 건너뛴 수를 API로 노출해 조용히 묻히지 않게 한다.
  - **잠금**: FastAPI의 동기 엔드포인트는 스레드풀에서 실행되므로 폴링이 잦아지면 요청이 겹칠 수 있고, 그러면 읽기 위치가 어긋나거나 같은 줄을 두 번 먹는다. 상태를 만지는 구간을 `threading.Lock`으로 감싼다.
  - **LIVE 표시 복원**: 2026-07-22에 제거했던 LIVE 표시를 되살린다. 그때는 정적 스냅샷이라 표시가 사실과 어긋났고, 지금은 서버가 실제로 텔레메트리를 따라가므로 사실과 일치한다. 표시의 정당성은 표시 자체가 아니라 그 뒤의 동작에서 나온다.
  - **폴링 방식**: `setInterval`이 아니라 매번 `setTimeout`을 새로 건다. `setInterval`은 응답이 늦어도 다음 요청을 그대로 쏘아 서버가 밀리면 요청이 쌓인다. 탭이 가려지면(`visibilitychange`) 폴링을 멈춘다.
  - **선택 유지**: 상세 패널의 선택을 사건 객체가 아니라 사건 번호로 들고, 매 렌더에서 현재 목록에서 찾는다. 객체를 붙들면 1초마다 참조가 끊겨 패널이 닫힌다. 번호로 들면 진행 중 사건의 지속 시간과 프레임 수가 패널에서도 실시간으로 늘어난다.
- 2026-07-23: 사건을 세션 파일(JSONL)로 내보낸다.
  - **동기**: 서버가 죽으면 메모리의 사건이 전부 사라진다. 이 프로젝트가 잡으려는 대상 중에 크래시와 행이 있는데(워치독 오라클) 정작 기록 시스템이 크래시에 다 날아가면 앞뒤가 맞지 않는다.
  - **세션 종료 시 일괄 덤프가 아니라 사건이 닫힐 때마다 한 줄씩 덧붙인다.** 프로세스가 언제 죽어도 그때까지 닫힌 사건은 디스크에 남고 손실은 마지막 한 줄 이하다. 배열 하나로 감싼 JSON이면 닫는 괄호를 못 써서 파일 전체가 깨진다. JSONL은 한 줄이 깨져도 나머지가 읽히고, 텔레메트리가 이미 같은 형식이라 도구를 공유할 수 있다.
  - **집계기는 파일을 모른다.** `on_close` 콜백만 부르고, 그것을 파일 기록기에 연결하는 것은 서버의 몫이다. 검출기가 집계기를 모르는 것과 같은 이유로 의존 방향을 한쪽으로만 둔다.
  - **요약 파일에 검출 설정값을 함께 남긴다.** "이 결과가 어떤 경계값과 임계값으로 나왔는가"를 기록에 남기지 않으면 나중에 세션 파일만 봤을 때 판정의 근거를 재구성할 수 없다.
  - **진행 중 사건**: 닫히지 않은 사건은 세션 마감(`/api/export`, `/api/reset`, 게임 재시작 감지) 시점에 `ongoing` 표시를 달아 기록한다. 한계: 진행 중이던 사건은 프로세스가 갑자기 죽으면 유실된다. 매 폴링마다 별도 스냅샷 파일을 다시 쓰면 막을 수 있으나, 이득에 비해 복잡해 지금은 한계로 남긴다.
  - `flush()`는 매 사건마다 호출한다. 사건은 프레임과 달리 드물게 발생하므로 비용이 문제되지 않는다. 이는 프로세스가 죽는 경우를 막아 주며, 운영체제 자체가 죽는 경우까지 막으려면 `fsync`가 필요하지만 그 경우는 드물어 다루지 않는다.
- 2026-07-23: 세션 폴더에 **텔레메트리 원본 사본**을 남긴다.
  - **동기**: 게임은 `G_InitGame`에서 `trap_FS_FOpenFile(..., FS_WRITE)`로 텔레메트리를 열므로, 맵이 로드될 때마다 이전 내용이 지워진다(`docs/gamecode_instrumentation.md` 2.8). 덕분에 파일이 무한히 커지지 않는다는 이점이 있지만, 반대로 **맵을 바꾸는 순간 직전 매치의 원본이 영구히 사라진다.** 사건 기록만 남기면 결론은 남고 근거는 없어져, 임계값을 조정해 같은 매치를 재분석하는 일이 불가능해진다. 지금 구조에서 유일하게 복구 불가능한 손실 경로였다.
  - **처음 검토한 방식(폐기): 세션 마감 시 복사.** 맵 전환은 '파일 크기가 기억하던 오프셋보다 작아졌다'로 **사후에** 감지한다. 그 시점에는 원본이 이미 지워진 뒤이므로, 복사해 봐야 새 매치의 첫 몇 줄만 복사된다. 감지 방식 자체가 사후적이라 이 방향으로는 성립하지 않는다.
  - **채택한 방식: 읽으면서 동시에 복사.** 서버가 새 바이트를 읽을 때마다 그 바이트를 세션 폴더의 사본에 그대로 덧붙인다. 어차피 읽고 있는 내용이라 추가 비용이 사실상 없고(초당 13~26 KB), 맵이 언제 갑자기 전환돼도 그 직전까지가 이미 디스크에 있다.
  - **가공하지 않고 바이트를 그대로 쓴다.** 절반만 쓰인 줄이 섞여 있어도 손대지 않으며, 나머지 절반은 다음 읽기에서 이어 붙는다. 결과적으로 사본은 원본과 바이트 단위로 동일하다. 파싱해서 다시 쓰는 방식이면 숫자 표기가 미묘하게 달라져 사본이 원본이 아니게 된다.
  - **쓰는 순서가 중요하다.** 파일이 초기화된 폴링에서 읽은 바이트는 이미 '새 매치'의 것이므로, 세션 교체를 먼저 하고 그 다음에 사본을 쓴다. 순서가 뒤바뀌면 새 매치의 앞부분이 이전 매치 사본에 섞인다.
  - **`TailSource.poll()`의 반환을 `PollResult`(상태 목록·초기화 여부·원본 바이트)로 바꿨다.** 사본을 이 모듈이 직접 쓰지 않고 바이트만 넘기는 이유는 두 가지다. 읽는 책임과 쓰는 책임을 섞지 않기 위함이고, '이 바이트가 어느 세션의 것인가'는 세션을 관리하는 서버만 판단할 수 있기 때문이다.
  - **flush 정책이 사건 기록과 다르다.** 사건은 드물어 매번 flush 하지만, 텔레메트리는 초당 수십 KB가 흘러 매번 flush 하면 낭비다. 파일을 닫을 때 버퍼가 비워지는 것에 맡긴다. 대신 프로세스가 갑자기 죽으면 사본의 마지막 몇 KB가 유실될 수 있다.
  - **비용과 끄는 법**: 세션 폴더가 실제 플레이 시간에 비례해 늘어난다(봇 4마리 기준 시간당 약 46 MB). 원본과 달리 자동으로 지워지지 않으므로 오래된 세션은 직접 지워야 한다. `QA_ARCHIVE_TELEMETRY=0`으로 끌 수 있으나, 끄면 위의 손실 경로가 되살아난다.
  - 알려진 한계: 서버를 매치 도중 재시작하면 새 세션이 파일을 처음부터 다시 읽으므로 같은 구간이 두 세션 사본에 중복 저장된다. 손실이 아니라 중복이므로 그대로 둔다.
  - 검증: 1차 매치를 기록한 뒤 파일을 잘라 맵 전환을 흉내 냈다. 원본은 지워졌지만 사본은 10,126바이트가 남았고 **md5가 원본과 일치**했으며, 그 사본만으로 `run_invariants.py` 재분석이 정상 동작했다.
- 2026-07-23: 실제 게임 세션 5개를 분석해 **`g_gravity` 교란이 시험지로 부적합함을 확인했다.** 특징을 고치는 대신 시험지를 바꾸기로 한다.
  - 판별 방법: 파일 이름만으로는 어느 판인지 구분이 안 돼, **공중에 뜬 봇의 `vz` 감소율로 중력을 직접 역산**했다(`g = -dvz/dt`). 800/800/500/800/800이 정확히 나와 저중력 판이 특정됐다. 게임 파라미터를 텔레메트리에서 되짚어 낼 수 있다는 것 자체가 유용한 사실이다.
  - **관측: 저중력 500은 봇의 행동을 거의 바꾸지 않았다.** 특징 11개 전부에서 정상 대비 차이(0.0~0.29 표준편차)가 **정상 두 판끼리의 차이(0.0~0.22)와 같은 수준**이었다. 판마다 나는 흔들림 안이다.
  - **해석: 특징이 둔감한 것이 아니라 사실을 말하고 있었다.** 봇은 중력이 낮아져도 여전히 잘 걸어 다니고 경로도 효율도 비슷하다. 점프 궤적만 조금 달라진다. 이상탐지가 잡을 행동 변화가 애초에 없었다.
  - **`g_gravity`는 행동이 아니라 상수를 바꾼다.** 봇이 알아서 적응한다. 그리고 행동이 바뀔 만큼 극단으로 가면(300) `out_of_bounds`가 걸려 하드 규칙이 먼저 잡는다. 500에서는 규칙도 조용하고 행동도 그대로다. **중간 지대가 거의 없다.**
  - **`fall_accel`(하강 중 수직 가속도) 후보를 검토했으나 채택하지 않는다.** 유일하게 갈리는 값이었지만(정상 800.19 / 저중력 499.97) 정상 두 판에서 소수점까지 동일했다. **이것은 특징이 아니라 상수다.** 봇이 무엇을 하든 변하지 않고 설정을 바꿔야만 변한다. 그리고 "중력이 800이 아니면 이상"은 한 줄짜리 하드 인바리언트지 ML이 필요한 일이 아니다. **무엇을 찾는지 안다면 규칙이 낫다**는 원칙이 그대로 적용된다. 넣으면 저중력 시험은 100% 통과하지만 그것은 시험지에 정답을 적어 넣는 것이다.
  - **`g_spSkill`은 반영되지 않았다.** 해당 판의 특징이 정상과 구분되지 않았다. 봇 실력은 스폰 시점에 읽는 값이라 이미 접속한 봇에게는 콘솔 설정이 적용되지 않는다. `\kick allbots` 후 `\addbot <이름> <실력>`으로 다시 넣어야 한다.
  - **결론: 이상탐지가 맡을 자리는 행동이 이상해지는데 물리적으로는 가능한 영역이다.** 봇 실력 하향이 그쪽이다. 못 하는 봇은 헤매고 목표를 못 찾고 맴돌지만 물리 법칙은 하나도 어기지 않는다. 물리 파라미터 교란은 이 조건을 만족하기 어렵다는 것이 이번 실험의 소득이다.
  - 저중력 판은 버리지 않는다. "이 교란은 행동을 바꾸지 않는다"는 것도 기록할 가치가 있는 음성 결과이며, 오탐 검증용 정상 데이터로도 쓸 수 있다.
- 2026-07-23: **버그 2건을 발견해 수정했다.** 실제 게임에서 뽑은 세션 사본 5개를 분석하려다 드러났다.
  - **버그 1: 수동 내보내기 후 사본이 조각난 줄로 시작한다.** 5개 중 3개가 그랬다.
    - 원인: `TailSource`가 들고 있는 보류 중인 줄 조각의 소유권 문제다. 그 조각은 이전 세션 사본에 이미 기록돼 있고, 다음 읽기는 그 줄의 중간부터 시작한다. 그래서 새 사본의 첫 줄이 조각난 채로 남는다.
    - **맵 전환에는 이 문제가 없다.** 파일이 잘리면서 버퍼도 함께 비워지기 때문이다. 사본 쓰기 순서를 설계할 때 맵 전환 경우만 따졌고, 수동 내보내기 경우를 빠뜨린 것이 원인이다.
    - 조치: `TailSource.pending`으로 보류 조각을 노출하고, 새 세션을 시작할 때 그 조각을 새 사본 맨 앞에 넣는다. 몇 바이트가 두 사본에 겹치지만, 그래야 각 파일이 독립적으로 읽힌다.
    - **세션 교체 세 경우의 처리가 서로 다르다는 점이 핵심이다.** 수동 내보내기는 조각을 넣어야 하고(True), 수동 리셋은 `reset()`으로 버퍼가 비므로 넣을 것이 없으며(False), 게임 재시작 감지는 `poll()` 안에서 이미 버퍼를 비우고 새 파일을 읽었으므로 그 조각이 방금 읽은 `raw`에 이미 들어 있어 넣으면 중복된다(False). 이 차이를 `_start_session(seed_pending)`의 설명에 남겼다.
    - 데이터 손실은 각 사본당 첫 한 줄이며, 잘린 앞부분은 이전 세션 사본에 남아 있다.
  - **버그 2: 오프라인 로더가 깨진 줄에서 예외를 던진다.**
    - 원인: `TailSource`는 깨진 줄을 건너뛰고 세는데 `replay_source.iter_samples_from_jsonl`은 예외를 던진다. 같은 데이터를 실시간 경로는 견디고 오프라인 경로는 못 견디는 셈이다.
    - 이 때문에 `run_invariants.py`와 `tools/extract_features.py`가 실제 세션 사본을 하나도 읽지 못했다. **버그 1이 없었어도 언젠가 터졌을 문제다.** 텔레메트리는 게임이 기록하는 도중에 세션이 끝나면 마지막 줄이 항상 절반만 쓰인 상태로 남기 때문이다.
    - 조치: 깨진 줄을 건너뛰되 `on_skip` 콜백으로 알리고, 도구는 건너뛴 수와 줄 번호를 화면에 표시한다. 조용히 묻히면 데이터가 온전한지 알 수 없다. 파일을 열 때도 `errors="replace"`를 써서, 여러 바이트 문자 중간에서 잘린 줄 때문에 파일 전체를 못 여는 일을 막는다.
  - **교훈**: 두 경로(실시간·오프라인)가 같은 데이터를 다르게 다루면 한쪽에서만 터진다. 새 소스를 만들 때 기존 소스와 오류 처리 방침을 맞췄어야 했다.
  - 검증: 버그 재현 조건(절반짜리 줄이 남은 상태에서 내보내기)을 그대로 만들어 두 사본 모두 첫 줄이 온전함을 확인했다. 맵 전환 경로에서는 tick 중복이 생기지 않는 것도 함께 확인했다. 실제 업로드된 세션 사본으로 두 도구가 정상 동작하는 것도 확인했다.
- 2026-07-23: 이상탐지(②) 오라클의 검증 방법을 정했다.
  - **파라미터 교란이 핵심이다.** 주입기는 상태를 불가능한 값으로 오염시켜 하드 규칙에 걸리므로 이상탐지의 시험 데이터가 될 수 없다. 대신 게임에 이미 있는 cvar(`g_spSkill`, `g_gravity`, `g_knockback`)로 **상태를 오염시키지 않고 플레이 방식만** 바꾼다. 좌표는 맵 안, z는 바닥 위, 체력 정상, 잘 움직임 — 하드 규칙 5종이 전부 침묵하는데 이동 패턴은 확연히 다르다. 이것이 "규칙은 안 어기지만 정상은 아니다"의 실물이다.
  - **유효성 판정 기준**: 교란 세션에 하드 검출기를 돌려 **0건이면 유효한 시험 데이터**다. 뭔가 걸리면 교란이 과했다는 뜻이니 값을 조정한다. `g_speed`는 `impossible_velocity`에 걸릴 수 있어 제외한다.
  - 정답은 켠 시각으로 공짜로 딸려 온다. 하드 인바리언트를 주입기로 검증했던 방식과 같다.
  - **교란은 탐지 대상이 아니라 평가 재료다.** 모델은 "저중력을 찾아라"를 배우지 않는다. 정상만 배우고 벗어난 것에 점수를 준다. 저중력 세션은 그 점수가 실제로 오르는지 보는 시험지다. 만약 저중력을 잡는 것 자체가 목표라면 ML이 필요 없다. `g_gravity` 값을 직접 읽으면 된다. **안다면 규칙, 모르니까 ML이다.**
  - 따라서 **특징을 특정 교란에 맞춰 다듬지 않는다.** 수직 속도 분포를 넣어 "저중력 100% 탐지"를 얻어도, 실제로 만날 버그는 누가 중력을 잘못 설정한 것이 아니다. 시험 통과를 위해 특징을 고칠 때도 일반적인 값(예: 체공 시간 비율)으로 넣는다. 하나에만 쓸모 있는 특징과 여러 상황에 쓸모 있는 특징은 다르다.
  - **주 지표는 둘이 짝을 이룬다**: 미사용 정상 세션의 오탐률("정상을 정상이라 하는가")과 파라미터 교란("비정상을 비정상이라 하는가"). 전자만으로는 아무것도 말할 수 없다. 전부 정상이라고 답해도 오탐률은 0이다.
  - `qa_inject_stuck` 탐지 지연은 부가 지표로 쓴다. 하드 규칙은 2초를 기다린 뒤 잡으므로, 이상탐지가 그보다 먼저 반응하는지가 "그래서 뭐가 좋아지느냐"의 답이 된다.
  - **폐기한 안**: "끼임 임계값을 낮춰 규칙이 침묵하는 0.5~2.0초 구간을 검증 대상으로 쓴다". 애매한 현상으로 명확한 것을 검증하려 한 것이 문제다. 0.7초 멈칫이 버그인지부터 불분명해 결과가 나와도 해석되지 않는다. 또한 그 멈칫이 정상 봇 이동의 일부일 가능성을 확인하지 않고 드물다고 가정했다.
  - **맵 교차 검증은 하지 않는다.** 한 맵으로 고정하기로 했다. 그리고 현재 `BOUNDS`가 한 맵 기준이라 다른 맵에서는 하드 검출기가 오탐을 쏟아내므로, "하드 규칙 0건" 유효성 기준 자체가 성립하지 않는다.
- 2026-07-23: 이상탐지의 입력을 **윈도우 특징**으로 정하고 `qa/features.py`를 구현했다.
  - **왜 프레임이 아니라 윈도우인가**: 봇이 한 구역을 3분간 맴돌 때 그 3분의 모든 프레임은 개별적으로 완벽히 정상이다. 좌표도 속도도 체력도 정상 범위다. 프레임 단위 특징으로는 이상 신호가 데이터에 존재하지 않으므로 어떤 모델을 써도 잡을 수 없다. 같은 구간을 5초 창으로 요약하면 "이동은 많은데 순이동거리가 0에 가깝다"가 되어 뚜렷이 드러난다. 학습으로 해결되는 문제가 아니라 표현의 문제다.
  - 창 5초, 1초씩 겹쳐 이동. 5초는 `stuck_seconds`(2.0)보다 길어 규칙이 못 보는 영역을 보며, 20Hz에서 100프레임이라 통계가 안정적이다. 아직 정한 상수이며 교란 세션에서 3·5·10초를 비교하면 근거가 생긴다.
  - **위치를 직접 쓰지 않는다.** 좌표를 학습하면 모델이 "봇이 잘 안 가는 구석"을 이상으로 본다. 맵 끝 통로나 잘 안 쓰는 점프대는 드물지만 정상이다. 체력 규칙을 폐기하며 얻은 "드물다 ≠ 잘못됐다"와 같은 함정이다. 순이동거리·활동 반경 같은 파생량은 봇이 어디에 있든 같은 의미를 갖는다. 맵 의존성이 줄어드는 것은 부수 효과이지 주된 이유가 아니다.
  - 특징 11개를 네 그룹으로 둔다. A 이동 결과(net_displacement, path_length, move_efficiency), B 활동 범위·방향(radius_gyration, heading_change, direction_reversals), C 의도와 결과(input_ratio, moved_with_input, moved_without_input), D 수직(airborne_ratio, z_range).
  - **C 그룹이 이 프로젝트의 연장선이다.** 끼임 규칙을 "오래 정지"에서 "입력은 있는데 결과가 없음"으로 바꿨던 통찰을 이분법이 아니라 연속값으로 만든 것이다. `moved_without_input`은 그 거울상으로, 입력이 없는데 움직인다는 것은 외력에 밀리고 있다는 뜻이다.
  - **일부러 뺀 것**: 체력 변화는 정상 데스매치에서 교전 여부에 따라 극단적으로 흔들린다. 3초에 100이 깎이는 창도, 5분간 무피해인 창도 정상이다. 넣으면 모델이 "격전 중인 창"을 이상으로 보아 오탐률을 갉아먹는다. 발사(attack)는 텔레메트리에 명중·데미지 정보가 없어 해석할 수 없다. 명중률 계측을 추가하면 "쏘는데 아무 일도 안 일어남"이라는 좋은 신호가 되지만 QVM 재빌드가 필요하다.
  - 수평 거리만 쓴다. 낙하 시 z가 수천 단위로 변해 수평 이동량을 압도하기 때문이다. 수직은 D 그룹이 따로 담당한다.
  - 집계기와 같은 이유로 **상태 기계**로 만들었다. 지금은 파일 전체를 먹이고, 실시간으로 붙일 때는 새로 읽은 것만 먹이면 된다.
- 2026-07-23: 특징 설계 결함 하나를 발견해 수정했다. **`direction_reversals`를 연속된 두 프레임 사이의 90도로 정의했는데, 프레임 간격이 0.05초다.** 봇이 50ms 만에 90도를 꺾는 것은 물리적으로 거의 불가능하므로, 사실상 절대 걸리지 않는 특징이었다. 합성 데이터 검증에서 분산 0으로 드러났다. 같은 문제가 `heading_change`에도 있어, 직진하는 봇에서도 5초에 60도가 누적됐는데 이는 실제 전환이 아니라 프레임 단위 미세 흔들림의 합이었다. 방향을 0.25초(5프레임) 간격으로 표본화하도록 고쳤다. **특징을 정의할 때 그 값이 물리적으로 취할 수 있는 범위를 먼저 따져야 한다는 교훈이다.**
- 2026-07-23: `tools/extract_features.py`를 추가했다. 모델을 만들기 전에 특징이 실제로 어떤 값을 갖는지 확인하기 위한 도구다. 값이 전부 같거나(분산 0) 한쪽으로 몰려 있으면 학습에 기여하지 못하므로 미리 알아야 한다. 세션 둘을 넣으면 중앙값을 나란히 비교한다.
  - 검증(합성 데이터, 봇 4마리): 정상 대비 **맴돌기** 세션에서 `move_efficiency` −77%, `net_displacement` −76%, `radius_gyration` −60%, `heading_change` +758%로 A·B 그룹이 반응했다. **저중력** 세션에서는 `airborne_ratio` +1880%, `z_range` +1639%로 D 그룹이 반응했다. 서로 다른 그룹이 서로 다른 교란에 반응하므로 시험지로 적절하다.
  - 미해결: `moved_without_input`이 합성 데이터에서 분산 0이다. 합성 봇은 입력이 없을 때 속도도 0이라 그렇다. 실제 게임에서는 반동·관성으로 값이 생길 수 있으므로, 실제 세션으로 다시 확인해야 판단할 수 있다.
- 2026-07-23: 프레임 간격을 실측했다. **가정이 사실로 확인됐다.**
  - 결과: 실제 게임 텔레메트리(봇 2마리, 53.1초, 2,125줄)에서 봇별 연속 기록 간격이 **0.050초로 100% 균일**했고, 초당 20.0프레임이 역산됐다. 기록이 끊긴 구간은 0건이었다.
  - 따라서 `gap_seconds=0.5`는 정확히 10프레임이며, "20Hz면 0.05초 주기이니 0.5초는 약 10프레임"이라는 근거가 사실로 뒷받침됐다.
  - **값은 바꾸지 않는다.** 도구는 0.1초를 권했으나, 그 근거는 "관측된 흔들림 바로 위"이고 이번 세션에는 흔들림도 공백도 없어 판단할 재료가 없었다. 반면 0.5초의 근거는 "탐지가 몇 프레임 깜빡이는 경우를 묶는다"인데, 그런 깜빡임이 이 데이터에 없었을 뿐 없는 현상은 아니다. 0.1초는 2프레임 여유라 3프레임만 깜빡여도 사건이 쪼개진다. 실측이 확정한 것은 환산이지 10프레임이 과하다는 사실이 아니다.
  - **간격이 흔들림 없이 완벽히 균일한 이유**를 확인했다. Quake3 계열은 `level.time`을 서버 프레임마다 `1000/sv_fps` ms씩 **고정 증가**시킨다. 즉 텔레메트리의 `time`은 실제 시간이 아니라 **시뮬레이션 시간**이다. 서버가 실제로 2초간 멈춰도 `level.time`은 50ms만 흐른다.
  - **여기서 따라오는 결론(중요)**: 현재 텔레메트리로는 성능 문제를 **원리적으로** 탐지할 수 없다. ③ 성능/크래시 워치독 오라클을 만들 때 이 텔레메트리를 재활용할 수 없고, 실제 시각(wall clock)을 기록하도록 게임 계측을 한 번 더 손대야 한다. 워치독 착수 전에 알아 둬야 할 제약이다.
  - **공백 0건의 해석**: 봇이 죽어도 `ClientEndFrame`이 계속 도는 것으로 보인다. "사망으로 사건이 갈라진다"는 앞선 우려는 이 데이터에서 근거가 없다. 다만 53초 동안 2마리가 한 번도 안 죽었을 가능성도 있어 단정하지 않는다.
  - 깨진 줄 1개는 세션 종료 시점에 반쯤 쓰인 마지막 줄이 사본에 그대로 복사된 것으로 보이며, 설계된 동작이다.
- 2026-07-23: 경계값·임계값을 `qa/config.py`로 통합한다.
  - 문제: 값이 `run_invariants.py`와 `dashboard/server/app.py` 두 곳에 각각 적혀 있어, 한쪽만 고치면 **같은 텔레메트리에 CLI와 서버가 다른 판정을 낸다.** 검출 결과가 도구에 따라 달라지면 그 결과를 신뢰할 근거가 사라진다.
  - **값만 모으지 않고 생성 함수(`build_checker()`, `build_aggregator()`)까지 둔다.** 값만 모으면 생성 코드가 여전히 두 곳에 남아, 인자 하나를 빠뜨리는 식으로 다시 어긋날 수 있다. 소비자가 함수만 부르면 그 여지가 없다.
  - 의존 방향: `config`는 `telemetry`·`invariants`·`aggregate`를 쓰지만 그 셋은 `config`를 모른다. 설정은 조립하는 쪽의 관심사이지 검출 로직의 관심사가 아니며, 이 방향을 지켜야 검출기를 다른 설정으로 테스트할 수 있다.
  - 서버에는 규칙과 무관한 운영 값(`RAW_KEEP`, `DEFAULT_LIMIT`)만 남긴다. 이것들은 검출 판정이 아니라 그 서버의 동작 방식이므로 공용 설정에 둘 이유가 없다.
  - `GAP_SECONDS` 주석에 실측 결과와 "값을 낮추지 않는 이유"를 함께 적었다. 값만 있고 근거가 없으면 나중에 누군가 도구의 권장값으로 무심코 바꾼다.
  - 맵 의존성은 한계로 남긴다. 지금은 한 맵 기준 단일 설정이며, 나중에 맵 이름을 키로 하는 표로 바꾸기 쉽도록 값들을 모듈 상단에 평평하게 늘어놓았다.
  - 검증: 같은 텔레메트리로 CLI와 서버가 모두 원시 탐지 237건 → 6개 사건으로 일치했고, 세션 요약의 `config` 항목에 통합된 설정이 그대로 실렸다.
- 2026-07-23: `tools/measure_interval.py`를 추가한다. 집계의 `gap_seconds`를 실측으로 뒷받침하기 위한 도구다.
  - 재는 방법: 게임은 `ClientEndFrame`에서 봇마다 한 줄씩 기록하므로, **같은 봇의 연속된 두 줄 사이 `time` 차이**가 서버 프레임 주기다. 봇을 나눠서 보는 것이 핵심이다. 봇을 섞으면 같은 서버 프레임 안의 줄 사이 간격(0초)이 끼어들어 값이 망가진다.
  - 대표값으로 평균이 아니라 **중앙값**을 쓴다. 프레임이 가끔 밀려 큰 값이 섞이는데, 평균은 그 이상치에 끌려간다.
  - **공백 탐지가 실질적으로 더 중요하다.** 프레임 주기의 3배를 넘는 간격은 기록이 끊긴 구간(봇 사망 등)으로 본다. 이 공백이 `gap_seconds`보다 크면 하나의 버그 사건이 둘로 갈라지므로, `gap_seconds`를 정하는 기준은 프레임 주기 자체보다 공백의 크기다.
  - 3배로 잡은 근거: 서버 프레임이 한두 번 밀리는 것은 정상 범위의 흔들림이지만, 세 프레임 이상 통째로 비었다면 기록이 멈춘 것으로 보는 편이 타당하다.
  - 권장값은 정상 간격 최댓값의 2배로 제시하되, **정상 흔들림의 상단과 공백의 하단이 겹치면 충돌 경고를 낸다.** 그 경우 어떤 값을 골라도 사건이 갈라지거나 붙는 문제가 남는다. 공백이 사망·리스폰이라면 그 구간을 하나로 묶는 것이 오히려 부적절할 수 있으므로, 갈라지는 쪽을 택하는 편이 나을 수 있다는 점을 함께 안내한다.
  - 도구는 값을 제안할 뿐 결정하지 않는다. 분포와 공백 목록을 함께 출력해 사람이 판단하게 한다.
  - 검증: 합성 데이터(봇 4마리 20Hz, 봇 하나가 4.2초간 사망, 3% 확률로 프레임 밀림)로 확인했다. 중앙값 0.050초에서 초당 20.0프레임을 역산했고, 4.2초 공백을 사망 구간으로 지목했다. 충돌 경고 분기와 인자 없음·빈 파일 처리도 확인했다.
- 2026-07-23: 실행 스크립트 `run_qa.bat` / `stop_qa.bat`를 프로젝트 루트에 추가한다.
  - 배경: 실행에 필요한 것이 conda 환경 활성화, uvicorn, vite 세 가지로 늘어나 매번 손으로 띄우기 번거로워졌다.
  - `conda activate`는 실행 파일이 아니라 배치 파일이므로 `call` 없이 부르면 제어가 돌아오지 않고 스크립트가 끝난다. 또한 Anaconda를 PATH에 추가하지 않고 설치하면 일반 cmd에서 `conda`가 잡히지 않으므로, 흔한 설치 경로에서 `condabin\conda.bat`을 찾는 대체 경로를 둔다.
  - uvicorn과 vite는 둘 다 상주 프로세스라 한 창에서 순서대로 실행하면 앞의 것이 뒤를 막는다. `start`로 각각 별도 창에 띄우고 창 제목을 붙여, `stop_qa.bat`가 제목으로 찾아 `/T`(자식 포함)로 종료한다. 프로세스 이름(`node.exe` 등)으로 죽이면 무관한 작업까지 종료되기 때문이다.
  - 텔레메트리 경로는 부모 스크립트에서 환경변수로 설정한다. 자식 창이 환경을 물려받으므로, 자식 명령줄 안에서 `set`을 해 따옴표가 겹치는 문제를 피할 수 있다.
  - `--reload`는 쓰지 않는다. 파일이 바뀔 때마다 서버가 재시작되면서 실시간 감시 상태가 통째로 날아간다.
  - 한계: 작성자가 Windows 환경에서 실행 검증을 하지 못했다(리눅스 컨테이너에는 conda와 cmd가 없다). 라벨/goto 정합성과 코드 페이지 순서만 정적으로 확인했다.
- 2026-07-23: `HANDOFF.md`를 제거하고, 그 역할(현재 구조 스냅샷)을 `docs/explain.md`가 겸하게 한다.
  - 사유: HANDOFF는 "서버 실행 확인 미확인", "리액트 대시보드(mock 데이터)", "바로 다음 할 일: 대시보드를 API에 연결" 등 이미 끝난 작업을 그대로 담고 있어, 새 세션에 넘기면 완료된 일을 다시 시키게 된다.
  - `process.md`만으로는 대체되지 않는다는 점을 고려했다. `process.md`는 append-only 시간순 기록이라 취소선이 섞여 있어 "지금 무엇이 참인가"를 읽어 내는 데 품이 든다. 그래서 `explain.md`에 실행 방법과 남은 작업·알려진 한계를 함께 담아 스냅샷 역할을 부여했다.
  - 역할 분담: `explain.md`는 현재 구조, `process.md`는 결정과 그 이유, `gamecode_instrumentation.md`는 게임 소스 변경, `injection_map.md`는 주입 지점 지도다.
- 2026-07-23: `docs/explain.md`를 전면 갱신했다. 갱신 과정에서 **이 문서가 제 작업 이전부터 이미 틀려 있었음**을 확인했다. 3.4의 체력 규칙 설명이 `health < 0` 또는 `health > max_health`로 되어 있었는데, 이는 2026-07-20에 오탐으로 폐기한 옛 기준이다(현재는 `health > max_possible_health`만 보며 음수 검사는 없다). 폐기 경위는 `process.md`에 남아 있었으나 `explain.md`에 반영되지 않았다. 문서가 여러 개일 때 한쪽만 갱신하면 이런 어긋남이 생긴다는 것을 기록해 둔다.
- 2026-07-23: `run_invariants.py`의 기본 출력을 사건 단위로 바꾸고 `--raw` 옵션을 추가한다. `stuck`의 `reported` 플래그를 제거하면서 CLI가 끼임을 매 프레임 출력하게 된 회귀를 함께 해소한다. CLI와 서버가 같은 집계 층을 쓰므로 두 경로의 결과가 일치한다.
- 2026-07-22: `out_of_bounds` 규칙의 `details`에 `overshoot`(경계를 가장 많이 넘어선 거리)을 추가한다. 다른 규칙은 위반 수치가 단일 값이라 극값을 고를 수 있는데, 경계 이탈만 x·y·z 세 값이라 어느 쪽이 더 심한 이탈인지 비교할 수 없었다. 판정에 필요한 축별 초과량은 규칙이 이미 계산하고 있으므로, 그 최댓값을 함께 실어 보내는 것으로 해결했다.

---

## 5. 열린 질문 / 미정 사항
- 액터를 OpenArena 내장 봇으로 갈지, 스크립트 봇으로 갈지 미정이다. (2026-07-20 기준 검토 중)
- 이상탐지 모델 선택과 특징(feature) 설계가 미정이다.
- ~~대시보드 구현 방식(웹 / 데스크톱)이 미정이다.~~ `[제거 2026-07-20: 리액트(웹) + FastAPI로 확정, 결정 로그 참조]`
- ~~합성 버그의 구체적 목록과 주입 방식이 미정이다.~~ `[제거 2026-07-20: 주입 방식은 결정 로그에서 확정함]`
- 합성 버그의 구체적 목록이 미정이다. (주입 방식은 확정, 유형별 목록은 각 오라클 구현과 짝지어 점진적으로 확정한다) `[추가 2026-07-20]`

---

## 6. 구현 진행 경과 (2026-07-20 기준)
지금까지 완료한 마일스톤은 다음과 같다.
- 하드 인바리언트 검출기(오라클 ①)를 Python으로 구현했다. 규칙 5종(바닥 관통·경계 이탈·체력 범위·과속·끼임)과 `InvariantChecker`, 리플레이 소스, 진입점을 만들고 합성 텔레메트리로 5건 검출을 확인했다.
- Windows에 OpenArena 0.8.8을 설치하고, `OpenArena/gamecode`를 QVM으로 빌드해 `qa` 모드로 적재하는 파이프라인을 검증했다. (빌드→배포 자동화 배치 파일 포함)
- 코드 수정이 게임에 반영되는 루프를 검증했다(`G_InitGame`에 로그 한 줄 추가로 확인).
- 첫 결함 주입기 `qa_inject_health`를 구현하고, 콘솔에서 토글 시 봇/플레이어 체력이 999로 고정되는 것을 확인했다.
- 텔레메트리 훅을 구현했다. `ClientEndFrame`에서 봇 상태를 JSONL로 파일에 기록하며, 주입값이 그대로 실려 나가도록 주입 직후에 배치했다.
- 엔드 투 엔드 흐름을 실제 게임에서 검증했다. 봇 매치에서 `qa_inject_health`를 토글해 얻은 실제 텔레메트리를 `run_invariants.py`에 넣어, 주입 구간의 999가 `health_out_of_range`로 검출되는 것을 확인했다. 게임 → 텔레메트리 → 검출기 → 판정의 첫 수직 흐름이 완성됐다.
- 실제 데이터로 체력 규칙의 오탐을 발견하고 교정했다(결정 로그 참조). 정상 오버힐(체력 95 등)이 더 이상 잡히지 않고 주입값 999만 잡히는 것을 확인했다.
- 맵 경계를 `tools/calibrate_bounds.py`로 정상 플레이 텔레메트리에서 산출해 `run_invariants.py`에 반영했다. 그 결과 `out_of_bounds` 오탐이 정상 구간에서 0건이 됐다(음성 검증 통과).
- 경계 이탈 주입기 `qa_inject_oob`를 추가하고, 봇을 x=100000으로 강제했을 때 `out_of_bounds`가 잡히는 양성 검증을 확인했다.
- 바닥 관통 주입기 `qa_inject_fall`를 콜리전 제거(tracemask에서 `CONTENTS_SOLID`·`CONTENTS_BOTCLIP` 제거) 방식으로 구현했다. 봇이 중력 가속대로 실제 추락하며 z가 내려갔고, `fell_through_floor`가 잡혔다. 이 방식은 화면 시연(자연 낙하)과 검출을 동시에 만족한다. 낙하 속도가 커지며 `impossible_velocity`도 함께 잡혔는데, 이는 자유낙하의 정상 반응이지 오탐이 아니다.
- 끼임 규칙을 "오래 정지"에서 "이동 입력이 있는데 위치가 안 변함"으로 개선했다(텔레메트리에 `move_input` 추가, 검출기에 입력 게이트 적용). 정상 대기(입력 없음)는 더 이상 오탐되지 않아 정상 플레이에서 0건이 됐다.
- 끼임 주입기 `qa_inject_stuck`를 추가했다(이동 입력은 살리고 위치만 고정). 켰을 때 봇들이 `stuck`으로 검출돼 양성 검증을 통과했다.

현재 검증 상태(갱신): **검출기 다섯 규칙(체력·경계·바닥 관통·과속·끼임)이 모두 양성·음성 검증을 통과했다.** 하드 인바리언트 오라클(①)이 실제 게임 데이터로 완결됐다.

표현 층(대시보드) 진행:
- 리액트 대시보드(mock 데이터) 1차 완성. 3열 구조(좌: 규칙별 집계 + 이상탐지 자리 / 중앙: 실시간 탐지 로그 / 우: 상세 보기 옆 패널), 인광 그린 계기판 톤. 브라우저에서 렌더 확인. 데이터 소스 블록을 표시해 나중에 파이썬 API 폴링으로 교체할 자리를 남겼다.
- FastAPI 서버(`dashboard/server/app.py`) 작성. 텔레메트리를 읽어 `InvariantChecker`를 돌리고 `/api/detections`로 버그 목록을 내보낸다. 각 탐지에 그 순간의 텔레메트리 값(좌표·속도·체력 등)을 실어 상세 패널이 바로 쓰게 했다. 정적 버전(파일 전체를 한 번에 검출)으로 시작한다. 컨테이너에서 검출 로직 동작을 확인했다.
- 주의: 서버의 경계·임계값이 `run_invariants.py`와 중복돼 있다. 향후 `qa/config.py` 공용 설정으로 통합한다.

~~다음 목표(갱신 2): (1) 대시보드의 mock 데이터 자리를 파이썬 API 요청으로 교체해 실제 검출 결과를 화면에 띄운다(정적 연결). (2) 그 위에 실시간(새 줄만 읽기)과 집계(중복 묶기)를 얹는다. (3) 남은 오라클(이상탐지, 성능/크래시 워치독)은 이후로 미룬다.~~ `[제거 2026-07-22: (1) 완료로 갱신]`

`[추가 2026-07-22]` 표현 층 진행(이어서):
- 사용자가 `uvicorn app:app --reload`로 서버를 띄워 `http://127.0.0.1:8000/api/detections`에서 JSON이 나오는 것을 확인했다. 서버가 실제 환경에서 동작함이 검증됐다.
- 대시보드의 mock 데이터 블록을 파이썬 API 요청으로 교체했다(정적 연결). 화면이 뜰 때 `/api/detections`를 한 번 받아와 그리고, 다시 불러오기 버튼으로 재요청한다.
- 서버 응답을 화면용으로 바꾸는 변환 함수 `normalizeDetection`을 두어 필드 이름 불일치를 흡수했다(결정 로그 참조).
- 연결 실패 상태를 화면에서 구분해 표시하도록 했다. 서버 미기동(fetch 자체 실패)과 서버 오류 응답(비정상 상태 코드)을 다른 문구로 안내하고, 탐지 0건인 경우와도 구분한다. 빈 화면의 이유를 화면만 보고 알 수 있어야 하기 때문이다.
- LIVE 표시·세션 시계를 제거하고 연결 상태 + 다시 불러오기로 대체했으며, mock 잔재(봇 이름·맵 이름)를 제거했다(결정 로그 참조).
- 리액트를 먼저 켜고 서버를 나중에 켰을 때 수동 새로고침이 필요했던 문제를 자동 재시도로 해결했다. 연결 자체가 안 되는 경우에만 3초 간격으로 재시도하며, 서버 오류 응답은 수동 재시도로 남겼다(결정 로그 참조). 데이터 갱신용 주기 폴링은 서버 실시간화 이후로 미뤘다.

`[추가 2026-07-22]` 집계 층 구현:
- 집계 층 `qa/aggregate.py`를 구현했다. `BugAggregator`는 탐지를 하나씩 받아 `(봇, 규칙)` 단위 사건으로 묶는 상태 기계이고, `BugEvent`는 시작/끝·지속·프레임 수·시작 순간 상태·규칙별 극값·진행 중 여부를 담는다. 상태 스냅샷 변환(`snapshot`)을 이 모듈에 모아 서버의 두 엔드포인트가 같은 형태를 쓰게 했다.
- `stuck`의 `reported` 플래그를 제거하고, `out_of_bounds`의 `details`에 `overshoot`을 추가했다(결정 로그 참조).
- 서버에 `/api/events`를 신설하고 `/api/detections`(원시)를 유지했다. 검출과 집계를 한 번의 순회로 처리해 파일을 두 번 읽지 않는다.
- 대시보드를 사건 단위로 바꿨다. 목록의 각 행이 사건 하나이며 지속 시간·프레임 수·진행 중 여부를 함께 보여준다. 상세 패널은 시작 순간의 상태와 가장 심했던 순간을 나눠 표시한다. 헤더에 사건 수와 원시 탐지 수를 함께 두어 얼마나 묶였는지가 드러나게 했다.
- 컨테이너에서 합성 텔레메트리(연속 낙하 40프레임 → 정상 복귀 2초 → 두 번째 낙하 20프레임, 끼임 구간, 체력 오버플로우 구간)로 검증했다. 원시 탐지 237건이 6개 사건으로 묶였고, 두 번의 낙하가 서로 다른 사건으로 분리됐으며, 스트림 끝까지 이어지던 마지막 사건만 진행 중으로 표시됐다.
- 확인한 불일치(미해결): `run_invariants.py`의 `MapBounds`·`max_speed`가 데모 초기값(±1000, 700)으로 남아 있어 서버의 보정값(min_x=-852 등, 1214)과 다르다. 이 상태에서 CLI와 서버는 같은 텔레메트리에 다른 판정을 낼 수 있다. `qa/config.py` 통합 작업에서 함께 해소한다.

`[추가 2026-07-23]` 실시간화와 세션 기록:
- `qa/tail_source.py`를 만들었다. 파일 오프셋을 기억해 새 줄만 읽고, 반쯤 쓰인 줄을 버퍼에 남겼다가 다음 읽기에서 이어 붙이며, 파일이 잘리면 초기화를 감지해 알린다. 바이트 모드로 읽어 잘린 문자 문제를 피한다.
- `qa/session_log.py`를 만들었다. 사건이 닫힐 때마다 JSONL에 한 줄씩 덧붙이고, 세션 마감 시 진행 중 사건과 요약(검출 설정값 포함)을 남긴다.
- `qa/aggregate.py`에 `on_close` 콜백과 `open_events()`를 추가했다. 집계기는 파일을 모르고 사건이 닫혔다는 사실만 알린다.
- 서버를 실시간 구조로 바꿨다. 감시 상태(`LiveSession`)를 요청 사이에 유지하고, 상태를 만지는 구간은 잠금으로 감쌌다. 엔드포인트를 `/api/events`, `/api/detections`, `/api/session`, `/api/export`, `/api/reset`으로 정리했다. 원시 탐지는 최근 500건 링 버퍼로 제한하고 누적 수를 함께 노출한다.
- 대시보드를 1초 폴링으로 바꾸고 LIVE 표시를 되살렸다. 탭이 가려지면 폴링을 멈추고, 상세 패널의 선택을 사건 번호로 유지해 진행 중 사건의 값이 실시간으로 갱신되게 했다. 헤더에 내보내기·새 세션 버튼을 추가했다.
- `run_invariants.py`를 사건 단위 출력으로 바꾸고 `--raw`를 추가했다.
- 세션 폴더에 텔레메트리 원본 사본(`session_<시각>.telemetry.jsonl`)을 남기도록 했다. 읽는 즉시 바이트를 그대로 복사하므로 맵 전환으로 원본이 지워져도 사본이 남는다. 요약 파일에 사본 이름과 크기를 기록하고, 대시보드 푸터에 현재 사본 크기를 표시한다.
- 컨테이너 검증: (1) 파일에 줄을 나눠 덧붙이며 증분 읽기 확인, (2) 일부러 절반만 쓴 줄이 다음 읽기에서 유실 없이 처리됨(처리 줄 수 50→51), (3) 사건이 닫히는 순간 세션 파일에 기록되고 진행 중 사건은 미기록으로 남음, (4) 파일을 잘랐을 때 새 세션이 시작되고 이전 세션 요약이 기록됨, (5) 별도 스레드가 계속 쓰는 동안 6회 폴링하며 사건 번호가 유지되고 진행 중 사건의 프레임 수가 20→300으로 증가함, (6) 원시 탐지 링 버퍼가 한도 내에서 유지됨.

`[추가 2026-07-23]` 실제 게임 검증과 문서 정리:
- 사용자가 실제 게임에서 실시간 경로 전체(계측 → 꼬리 읽기 → 검출 → 집계 → 서버 → 대시보드 → 세션 기록)가 정상 동작함을 확인했다. 그때까지 집계 이후의 작업은 전부 합성 데이터로만 검증된 상태였다.
- 미확인 항목: 실측 프레임 간격을 보고받지 못했다. 집계의 `gap_seconds=0.5`는 "20Hz면 10프레임"이라는 가정 위에 서 있으므로, 실측값으로 근거를 굳히는 일이 남아 있다.
- 실행 스크립트 `run_qa.bat` / `stop_qa.bat`를 추가했다.
- `HANDOFF.md`를 제거하고 `docs/explain.md`를 전면 갱신해 현재 구조 스냅샷 역할을 맡겼다.

~~다음 목표(갱신 5): (1) 서버와 `run_invariants.py`에 중복된 경계·임계값을 `qa/config.py` 공용 설정으로 통합한다(현재 값은 맞춰 두었으나 두 곳에 적혀 있어 한쪽만 고치면 어긋난다). (2) 이상탐지(ML) 오라클에 착수한다. 학습 데이터는 이제 세션 폴더에 쌓이는 텔레메트리 사본을 그대로 쓸 수 있다. (3) 성능/크래시 워치독 오라클은 그 이후로 미룬다.~~ `[제거 2026-07-23: 실제 게임 검증 완료와 문서 정리를 반영해 갱신]`

~~다음 목표(갱신 6): (1) 실측 프레임 간격을 재어 `gap_seconds`의 근거를 확정한다. (2) 경계·임계값을 `qa/config.py` 공용 설정으로 통합한다. (3) 이상탐지(ML) 오라클에 착수한다. (4) 성능/크래시 워치독 오라클은 그 이후로 미룬다.~~ `[제거 2026-07-23: (1)(2) 완료]`

다음 목표(갱신 7): (1) **이상탐지(ML) 오라클**에 착수한다. 남은 것 중 가장 큰 덩어리다. 학습 데이터는 주입을 끈 채 몇 판 돌리면 세션 폴더의 텔레메트리 사본으로 쌓인다. (2) 성능/크래시 워치독 오라클. 착수 전에 게임 계측에 실제 시각 기록을 추가해야 한다(위 실측 항목 참조). (3) 표현 층을 PyQt로 옮기는 것은 선택 사항이다. 서버는 JSON HTTP API 소비자만 바꾸면 되므로 백엔드 변경이 없다.

~~다음 목표(갱신 4): (1) 서버가 텔레메트리 파일 끝에서 새 줄만 읽도록 바꿔 실시간화하고, 대시보드에 주기 폴링과 실시간 표시를 되살린다. (2) 서버와 `run_invariants.py`에 중복·불일치한 경계·임계값을 `qa/config.py` 공용 설정으로 통합한다. (3) 남은 오라클(이상탐지, 성능/크래시 워치독)은 그 이후로 미룬다.~~ `[제거 2026-07-23: (1) 실시간화 완료로 갱신]`

~~다음 목표(갱신 3): (1) 서버가 텔레메트리 파일 끝에서 새 줄만 읽도록 바꿔 실시간화한다. (2) "같은 봇 같은 규칙 연속"을 하나의 버그 사건으로 묶는 집계를 서버 층에 추가한다. (3) 서버와 `run_invariants.py`에 중복된 경계·임계값을 `qa/config.py` 공용 설정으로 통합한다. (4) 남은 오라클(이상탐지, 성능/크래시 워치독)은 그 이후로 미룬다.~~ `[제거 2026-07-22: (2) 집계 완료로 갱신]`

다음 목표(갱신 3): (1) 서버가 텔레메트리 파일 끝에서 새 줄만 읽도록 바꿔 실시간화한다. (2) "같은 봇 같은 규칙 연속"을 하나의 버그 사건으로 묶는 집계를 서버 층에 추가한다. (3) 서버와 `run_invariants.py`에 중복된 경계·임계값을 `qa/config.py` 공용 설정으로 통합한다. (4) 남은 오라클(이상탐지, 성능/크래시 워치독)은 그 이후로 미룬다.

~~다음 목표(갱신): 하드 인바리언트 오라클이 완결됐으므로, (1) 매 프레임 중복 검출을 하나의 "버그 사건"으로 묶는 집계 층(Bug Store), (2) 봇 관전 화면 + 실시간 대시보드(발표용 표현 층)로 진행한다. 집계가 대시보드의 전제이므로 집계를 먼저 한다. 방향은 검토 중이다.~~ `[제거 2026-07-20: 대시보드 착수·서버 작성으로 갱신]`

---

## 7. 변경 이력
- 2026-07-20: 문서 최초 작성.
- 2026-07-20: 합성 버그 주입 방식 확정(cvar 토글 결함 주입기)을 결정 로그에 추가하고, 관련 열린 질문을 갱신했다.
- 2026-07-20: QVM 모드 빌드 확정, 주입·계측 위치(ClientEndFrame), 텔레메트리 파일 방식을 결정 로그에 추가하고, 구현 진행 경과(6절)를 신설했다.
- 2026-07-20: 엔드 투 엔드 검증 완료와 체력 하드 인바리언트 교정(절대 상한 방식)을 결정 로그·진행 경과에 기록했다. 남은 `out_of_bounds` 오탐과 맵 경계 보정 계획을 명시했다.
- 2026-07-20: 맵 경계를 관측(정상 플레이) 기반으로 산출하기로 한 결정과 근거를 결정 로그에 기록했다.
- 2026-07-20: 맵 경계 보정 완료, 경계 이탈·바닥 관통(콜리전 제거) 주입기 검증, 네 규칙 검증 완료를 진행 경과에 반영했다. 해결된 `out_of_bounds` 오탐 항목은 취소선 처리했다.
- 2026-07-20: 끼임 규칙을 입력 게이트 방식으로 개선하고 `qa_inject_stuck`으로 양성 검증까지 완료했다. 다섯 규칙 전부 양성·음성 검증 통과를 기록하고, 다음 목표를 집계 층·대시보드로 재정리했다.
- 2026-07-20: 표현 층 착수를 기록했다. 대시보드 방식(리액트+FastAPI, 게임은 별도 창, 3열 구조 등)과 맵 의존성 한계를 결정 로그에 추가하고, 리액트 mock 대시보드·FastAPI 서버 작성을 진행 경과에 반영했다. 대시보드 방식 관련 열린 질문을 해소 처리했다.
- 2026-07-22: 서버 동작 확인과 대시보드 정적 API 연결을 기록했다. 필드 이름 변환 방식, LIVE 표시 제거, 표시 행 수 제한, mock 잔재 제거를 결정 로그에 추가하고, 다음 목표를 실시간·집계·설정 통합으로 재정리했다.
- 2026-07-22: 연결 실패 한정 자동 재시도와 주기 폴링 보류 결정을 결정 로그·진행 경과에 추가했다.
- 2026-07-23: g_gravity 교란이 시험지로 부적합하다는 실험 결과와 fall_accel 후보를 채택하지 않은 이유를 기록했다.
- 2026-07-23: 사본 경계 버그와 오프라인 로더 예외 버그의 수정을 기록했다. 세션 교체 세 경우의 처리가 서로 다른 이유를 함께 남겼다.
- 2026-07-23: 이상탐지 검증 방법(파라미터 교란), 윈도우 특징 설계와 qa/features.py 구현, direction_reversals 설계 결함 수정, tools/extract_features.py 추가를 기록했다.
- 2026-07-23: 프레임 간격 실측 결과와 qa/config.py 통합을 기록했다. time 이 시뮬레이션 시간이라는 발견과 그것이 워치독 오라클에 주는 제약을 함께 남겼다.
- 2026-07-23: tools/measure_interval.py 추가를 기록했다. 봇별로 간격을 재는 이유, 중앙값을 쓰는 이유, 공백 탐지가 gap_seconds 결정의 실질적 기준인 이유를 남겼다.
- 2026-07-23: 실제 게임 검증 완료, 실행 스크립트 추가, HANDOFF.md 제거와 explain.md 전면 갱신을 기록했다. explain.md가 이전부터 폐기된 체력 규칙을 담고 있었던 사실도 함께 남겼다.
- 2026-07-23: 텔레메트리 원본 사본 보존을 기록했다. FS_WRITE로 인한 손실 경로, 사후 감지 때문에 '마감 시 복사'가 성립하지 않는 이유, 읽으면서 복사하는 방식과 쓰기 순서, 사본 무결성 검증 결과를 결정 로그에 남겼다.
- 2026-07-23: 실시간화와 세션 파일 기록을 기록했다. 메모리 관리 대상에 대한 앞선 판단(사건 보관 상한)을 철회하고 원시 탐지 쪽으로 정정한 경위, 실시간 읽기의 구현 방식(부분 줄·재시작 감지·잠금), JSONL 덧붙이기 방식의 근거, LIVE 표시 복원의 정당성을 결정 로그에 남겼다. run_invariants.py의 사건 단위 출력 전환도 함께 기록했다.
- 2026-07-22: 집계 층 구현을 기록했다. 집계 방식(묶는 기준·끊는 간격·극값 표·상태 기계 구조·엔드포인트 분리)과 stuck 플래그 제거, out_of_bounds overshoot 추가를 결정 로그에 남기고, 진행 경과와 다음 목표를 갱신했다. run_invariants.py와 서버의 임계값 불일치를 미해결 항목으로 명시했다.
