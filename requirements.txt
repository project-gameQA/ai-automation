# 게임 QA 자동화 - 필요 라이브러리
# 설치:  pip install -r requirements.txt
# (검증 버전 기준. 호환되는 버전이면 됨. torch는 불필요.)

numpy==2.4.4
gymnasium==1.3.0
minigrid==3.1.0
scikit-learn==1.8.0
joblib==1.5.3
vizdoom==1.3.0
PyQt6

# 선택(UI 다크 테마. 없어도 실행됨)
qt-material
